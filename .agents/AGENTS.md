# Project Rules for HNFOOT

1. **Testing Before Commit/Push**: Always perform automated tests (e.g. `python3 -m py_compile` for Python files) and verify code validity before pushing code.
2. **Auto Commit & Push**: After code changes are verified, automatically stage (`git add .`), commit with a clear Vietnamese or English commit message, and push (`git push origin main`) to the git repository.
